import { StyleSheet, Text, View } from "react-native";
import React from "react";

const Zoom = () => {
  return (
    <View>
      <Text>Zoom</Text>
    </View>
  );
};

export default Zoom;

const styles = StyleSheet.create({});
